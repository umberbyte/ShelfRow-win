# 継承コードの欠落分析と、今後の検証チェックリスト

2026-09-22 作成。Antigravity が実装した ShelfRow for Windows のコードを、実際に動く状態まで修復する過程で判明した欠落の傾向をまとめたもの。

このファイルの価値は「過去の批評」ではなく、**まだ触っていない領域に同じ穴が空いていると予測できること**、および**新規実装時に何を確かめれば同じ轍を踏まないか**にある。

## 前提: 何が良かったか

欠落の話に入る前に。継承したコードは以下の点で質が高く、作り直す必要は一切なかった。

- 5プロジェクトの分離(Core / Data / CloudKit / Importer / Storage)が適切
- `CD_` レコード型名、`com.apple.coredata.cloudkit.zone` などの命名規約が正確
- サムネイルの256シャード規約が mac 版と一致
- `CredentialLockerStorageService` は PasswordVault と DPAPI フォールバックを備えた良い実装
- mac 版 UI のレイアウト再現度が高い

問題は**構造ではなく、境界**にある。外部システム、実データ、実規模、フレームワークの実挙動——この4つの境界すべてで破綻していた。

---

## 傾向1: 形だけ正しく、契約が検証されていない

CloudKit マッパーは見た目が完全に正しかった。しかし実データをダンプすると3点が違っていた。

| 想定 | 実際 |
|---|---|
| `recordName` = モデルの `id` | 別物。モデルの id は `CD_id` に入る |
| `CD_volume` は CKReference | 対象の **recordName を入れた文字列** |
| 本棚の所属はレコードが持つ | **`CDMR` 結合レコード**に分離(19,217件) |

さらに `Convert.ToInt32(JsonElement)` で全レコードが例外になる状態だった。認証が通っていれば即座に発覚したが、認証が無いため到達しなかった。

**最も重要な点**: テストが実装と同じ思い込みを固定していた。

```csharp
// 間違った前提を「仕様」として固めていたテスト
Assert.Equal(item.Id.ToString("D"), ckRecord.RecordName);
```

24件のテストが100%通っていたのは、外部を一切見ずに自分の想定を検算していたため。**テストの緑は、外部契約については何も保証しない。**

## 傾向2: 簡単な9割があり、難しい1割が無い

全機能が「難所の手前まで」作られ、そこで止まっていた。

| 機能 | 実装済み | 欠落していた難所 |
|---|---|---|
| CloudKit 同期 | リクエスト/レスポンス型、マッパー、同期ループ | **認証**(ckAPIToken、サインイン経路) |
| ヘルパー起動 | 設定画面、拡張子マッピング | **`Process.Start`**(起動処理そのもの) |
| スマートシェルフ | JSON 保存、CloudKit 往復 | **条件の評価** |
| アップロード | `ToCKRecord` | **送信処理全体** |

欠落箇所は例外なく、外部システム・実データ・面倒な状態管理のいずれかに触れる。それでいて AGENTS.md には「完了済み」と記載されていた。

## 傾向3: エラーが構造的に見えない

**最もコストを増幅した傾向。** 全ての不具合が「何も起きない」という同一症状になり、毎回ゼロから探索することになった。

- `EnsureSuccessStatusCode()` が CloudKit のエラー JSON ごと破棄 — サインイン URL (`redirectURL`) はそこに入っていた
- GUI アプリで `Debug.WriteLine` — ユーザーにもログにも残らない
- `_ = StartAsync()` の投げっぱなし — WebView2 の初期化失敗が数往復ぶん見えなかった
- ガード節での無言 return — 「参照」ボタンはクリックが届いていたのに無反応
- ステータス表示が、操作中のウィンドウの**裏側**にしかない

### 教訓

早期 return と catch には必ず理由を残す。ログを出す位置は**ガード節より前**。GUI では、ユーザーが今見ている場所に結果を出す。

## 傾向4: 実データ・実規模で一度も動かしていない

フリーズ原因は4つとも「100冊なら問題ない」コードだった。19,287冊で破綻する。

1. `GridView` を `ScrollViewer` で包む → 縦方向が無限になり**仮想化が完全に無効化**
2. `ObservableCollection` に1件ずつ `Add` → 19,287回の変更通知
3. `GetItemsAsync(0, int.MaxValue)` で全件をメモリに
4. 同期処理を UI スレッドで実行(38,545レコードの JSON 解析と SQLite 書き込み)

AGENTS.md には「UI仮想化を適用」「UIスレッドを長時間ブロックする処理は絶対に実行しない」と明記されている。**方針は正しく、実装がそれを裏切っていた。** 方針を書くことと、満たしているか確かめることは別の作業。

## 傾向5: フレームワークの挙動を一般論で書いている

WinUI 3 固有の挙動に当たらず、XAML 一般の知識で書かれていた。

| 罠 | 症状 |
|---|---|
| `RatingControl` は「評価なし」を **-1** で表す。0 を渡すと1に丸めて書き戻す | 行を選ぶたび★1が付く |
| `ItemsRepeater` は実体化要素に `DataContext` を確実に設定しない | ボタンが全て無反応 |
| `ContentDialog` の既定最大幅は **548px** | 中身が切れる |
| `ListView`/`GridView` は Enter を処理済みにする | `KeyDown` が届かない |
| WinAppSDK 非パッケージ版は `RuntimeIdentifier` 必須 | `WebView2Loader.dll` が出力直下に来ず、PATH 上の別アーキテクチャ版を掴む |
| WebView2 は表示時に自前の環境生成を開始する | 後から環境を渡すと例外 |

示唆的なのは、AGENTS.md の「重要な知見」に同種の罠が2件記録されていること。**踏んだ記録はあるが、一般化されていない。**

## 傾向6: 部品はあるが、配線されていない

- `CredentialLockerStorageService` — 良い実装だが**呼ぶ側がいなかった**
- `VolumePathResolver` — ヘルパー起動を書くまで利用者ゼロ
- `CoverExtractionRecord` — 型だけで抽出処理が無い
- 同期ボタン — 空設定の `new CloudKitConfiguration()` を叩いていた

部品表は揃うので、進捗報告は「完了」になる。組み立てだけが無い。

---

## 根本原因

6つの傾向はすべて、**「いいえ」と言い返してくる相手に一度も当てていない**ことに帰着する。

本物の CloudKit、本物の19,287冊、本物の WinUI ウィンドウ。自分の想定に対して書いたテストは「いいえ」と言わない。だから完成して見え、肝心の経路はほぼ全て動かなかった。

膠着を破ったのが `tools/ShelfRow.CloudKitProbe`(実データをダンプする診断 CLI)だったのは偶然ではない。あれが最初の「いいえ」で、`recordName ≠ CD_id`、`CD_volume` が文字列、`CDMR` の存在はすべてあの1回で判明した。

---

## 新規実装時のチェックリスト

外部システムに触れる機能を書くとき:

- [ ] 実データを取得して構造を目視したか(想定で書いていないか)
- [ ] 認証・トークン・権限など、外部が「いいえ」と言う経路を通したか
- [ ] エラー応答の本文を読んでいるか(ステータスコードだけで捨てていないか)
- [ ] 失敗時にユーザーが**今見ている場所**に理由が出るか

実規模で動く機能を書くとき:

- [ ] 19,287冊で実行したか(100件で確かめて済ませていないか)
- [ ] UI スレッドを塞ぐ処理が無いか
- [ ] リストの仮想化が生きているか(`ScrollViewer` で包んでいないか)
- [ ] コレクション更新は一括差し替えか

WinUI 3 のコントロールを使うとき:

- [ ] そのコントロール固有の「未設定値」の表現を確認したか
- [ ] `ItemsRepeater` 内のイベントで `DataContext` に依存していないか(`Tag="{x:Bind}"` を使う)
- [ ] ダイアログの既定サイズ制約を超えていないか

常に:

- [ ] 早期 return と catch に理由のログを残したか
- [ ] `_ = SomethingAsync()` の投げっぱなしに try/catch があるか
- [ ] 部品を作ったら、呼ぶ側まで配線したか

## 未検証領域の予測

この傾向が正しいなら、まだ触っていない箇所も同じ穴を持つはず。着手時の答え合わせに使える。

| 領域 | 予測される欠落 | 該当傾向 |
|---|---|---|
| ZIP からの書影抽出 | `CoverExtractionRecord` 型のみで実装が無い | 2, 6 |
| サムネイルパイプライン | 19,287件規模で未検証、UI スレッド占有の恐れ | 4 |
| 本棚メンバーシップの送信 | CDMR 組み立てという難所が未着手(受信のみ実装済み) | 2 |
| Stackroom インポート | テストはあるが実際の `Stackroom Library.xml` で未検証 | 1 |

---

## 付録: 修復した不具合の一覧

証跡として。詳細は各コミットを参照。

**CloudKit 層**
1. `ckAPIToken` を一度も送っていない(全リクエストが401)
2. Web Auth サインイン経路が存在しない
3. エラー応答の本文を破棄(`redirectURL` が失われる)
4. `records/modify` に `zoneID` が無くデフォルトゾーン宛になる
5. `recordName` をモデルの id として扱う
6. `CD_volume` を CKReference として読み書き
7. `CDMR` 結合レコードを認識せず、本棚の所属が全消失
8. `Convert.ToInt32(JsonElement)` で全レコードが例外
9. アップロード経路が存在しない

**データ層**
10. CloudKit の recordName / changeTag を保持する列が無い
11. `GetItemsAsync` が `ShelfIds` を読まず、全ての棚が空になる
12. スマートシェルフの条件が保存されるだけで評価されない
13. 同期で降ってきた Volume が `WindowsMountPath` を上書き消去

**アプリ層**
14. 空設定で `CloudKitClient` を生成し、以後一切配線されない
15. API トークン入力欄もサインイン UI も無い
16. ヘルパー起動が未実装(ダブルクリック/Enter が死んでいる)
17. `RatingControl` の TwoWay バインドで★が勝手に付く
18. `GridView` を `ScrollViewer` で包み仮想化が無効
19. 全件をUIスレッドで読み込み、1件ずつ `Add`
20. 同期を UI スレッドで実行
21. `ItemsRepeater` 内で `DataContext` 依存のハンドラが全て無反応
22. `ContentDialog` が中身より狭くボタンが切れる
23. `RuntimeIdentifier` 未指定で `WebView2Loader.dll` がアーキテクチャ不一致
24. 例外の握り潰し(`Debug.WriteLine`、投げっぱなし async、無言 return)
